const { ConvexHttpClient } = require("convex/browser");
const client = new ConvexHttpClient("https://subtle-panther-507.convex.cloud"); // Assuming it's the prod URL, wait... no I don't know the prod URL.
// Actually I don't need a JS script for mutation if I can just use powershell correctly.
